# GuardianCI development dependencies.
#
# Install with:  uv pip install --system -r requirements/dev.txt
# Update with:   uv pip compile pyproject.toml --extra dev -o requirements/dev.txt
-r base.txt
pytest>=8.0.0
ruff>=0.9.0
